# Database Configuration
# PostgreSQL connection string
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/soltech

# Session Configuration (Replit Auth - será descontinuado)
SESSION_SECRET=your-session-secret-key-here

# Environment
NODE_ENV=development

# ==================== JWT AUTHENTICATION ====================
# JWT Secret Key - IMPORTANTE: Use uma chave segura em produção!
# Para gerar uma chave segura, use: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Access Token Expiration (formato: 15m, 1h, 24h, etc)
JWT_EXPIRES_IN=15m

# Refresh Token Expiration (em dias)
REFRESH_TOKEN_EXPIRES_DAYS=7

# Bcrypt Rounds (quanto maior, mais seguro mas mais lento)
# Recomendado: 10-12 para desenvolvimento, 12-14 para produção
BCRYPT_ROUNDS=12

# ==================== CONFIGURAÇÕES OPCIONAIS ====================

# Rate Limiting (proteção contra brute force)
# MAX_LOGIN_ATTEMPTS=5
# LOGIN_ATTEMPT_WINDOW_MINUTES=15

# Email Configuration (para envio de emails de verificação)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# SMTP_FROM=noreply@solarcontrol.com

# File Upload Configuration
# MAX_FILE_SIZE_MB=10
# UPLOAD_DIR=./uploads

# Cleanup Configuration
# PDF_RETENTION_DAYS=30
# CLEANUP_SCHEDULE=0 0 * * * (cron format)
